# Wild Eggs — ADA/WCAG Compliance Audit & Remediation Log

**Date:** 2026-07-11
**Source:** WAVE scan (AIM score 2.2/10 — 11 errors, 106 contrast errors, 16 alerts) + Framer project review via Framer MCP
**Site:** https://www.wildeggs.com (Framer project "wild eggs")

## Fixes applied in Framer (this session)

| Issue | WAVE finding | Fix | Status |
|---|---|---|---|
| Skipped heading levels (7 alerts) + 29 stray `h6`s | Uppercase "eyebrow" labels (e.g. "WHAT THE FLOCK IS SAYING") used the `/Heading 6` text style, which rendered as `<h6>` directly under `<h2>` sections | Changed `/Heading 6` text style tag from `h6` → `p` (visual appearance unchanged — same font, size, tracking) | ✅ Applied |
| Skipped heading levels (h2 → h4) | Card titles ("Breakfast is a mood…", "Wild through innovation.", etc.) used `/Heading 4` (`<h4>`) directly under `<h2>` sections | Changed `/Heading 4` text style tag from `h4` → `h3` | ✅ Applied |
| Very low contrast | `/Teal - Primary` color style was `rgba(47, 92, 100, 0.65)` — ≈2.5:1 on the cream background | Raised to full-opacity `rgb(47, 92, 100)` — ≈5.9:1, passes AA | ✅ Applied |
| Very low contrast | `/Grey` color style `rgb(113, 113, 113)` — ≈4.2:1 on the beige `rgb(250, 247, 240)` background, fails AA for normal text | Darkened to `rgb(90, 90, 90)` — ≈6.3:1 | ✅ Applied |

Notes:
- The `manageTextStyle` MCP tool briefly renamed `/Heading 6` to "Body" during the first update (tool bug); this was caught and reverted in the same session — the style is back at `/Heading 6` with identical metrics and the corrected `p` tag.
- Code components were audited for form-label errors and are already compliant: `HeroLocationSearch.tsx` (input + submit labeled, `role="search"`), `WildEggsNutritionCalculator.tsx` (all inputs/selects/filters have `aria-label`s), `PaylocityJobs.tsx` (iframe has `title`).

## Remaining items (blocked this session — need Framer-side follow-up)

The Framer MCP plugin could only serialize the **currently focused page** (the homepage). Every other page and every component (`Footer`, `Client Testimonial Card`, `Pill Label`, contact/waitlist/franchising pages, etc.) returned a serializer error ("Node is not a text node"), so these could not be fixed remotely:

1. **11 missing form labels** — these are Framer-native form inputs (contact, waitlist, franchising/catering forms, footer newsletter). In Framer: select each input → add a visible label or set the Name/aria-label on the input. Placeholder text alone does not count as a label.
2. **Remaining contrast errors (~106 instances)** — the two color-style fixes above will clear every instance bound to `/Teal - Primary` and `/Grey`, but instances with hard-coded colors inside components (testimonial cards, footer links on dark, orange accent text using `/Orange` `rgb(255,106,12)` / `/Theme Color 2` `rgb(255,131,66)`) need per-component review. Orange-on-cream fails AA (~2.4:1); if used as text, darken toward `rgb(178, 64, 0)` or reserve for large/decorative text.
3. **Very small text (1 alert)** — likely `/Body Small` (0.8rem) or a smaller one-off inside a component; bump to ≥0.875rem where flagged.
4. **Redundant link / noscript / HTML5 video alerts** — hero background video is decorative and muted; keep it `aria-hidden` and ensure it has no keyboard trap. Merge adjacent duplicate links pointing at the same URL.
5. **Duplicate color styles** — two styles share the path `/Grey` (113 → now 90, and a second at `rgb(153,153,153)`), and three share `/White`. The duplicate `/Grey` at 153 cannot be targeted via MCP; rename/merge duplicates in Framer's style panel.

**To finish remotely:** open the Framer project and focus each page (contact, waitlist, franchising, catering, locations) one at a time while an MCP session runs — the plugin can then read and fix that page. Then republish and re-run WAVE to verify the score.

## Verification

Re-run WAVE (https://wave.webaim.org) on https://www.wildeggs.com after the next publish. Expected impact of applied fixes: all 7 skipped-heading alerts cleared, heading outline h1→h2→h3 clean, and every contrast error bound to the two fixed color styles cleared.
